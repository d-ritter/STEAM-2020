function setup() {
  createCanvas(400, 400);
  background(220);
}

function draw() {
  
  var r = random(255);
  var g = random(255);
  var b = random(255);
  noStroke();
  fill(r, g, b);

  ellipse(mouseX, mouseY, 30, 30);
}
